version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Android!"

workflows:
  say-hello-android-workflow:
    jobs:
      - say-hello